# Static-link the native binary: no dynamic linker at startup (~40% faster
# process spawn). Safe here — the binary uses no NSS/DNS/getpw. Applies to the
# native/ crate (the only cargo project in this repo).
[build]
rustflags = ["-C", "target-feature=+crt-static"]
